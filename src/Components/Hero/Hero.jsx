import React from "react";
import "./Hero.css";

const boxCount = Array.apply(null, Array(100));

const Hero = () => {
  return (
    <>
      <div className="hero-img"></div>
    </>
  );
};

export default Hero;
